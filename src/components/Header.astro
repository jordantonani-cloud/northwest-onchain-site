---
/**
 * Sticky global header. Primary nav + "Book the Meet" CTA.
 * Mobile nav is an accessible disclosure: aria-expanded, Esc to close,
 * click-outside to close. The toggle script is bundled (not inline) so the
 * CSP can stay `script-src 'self'`.
 */
import { NAV, BOOKING } from '../consts';
import Logo from './Logo.astro';

const pathname = Astro.url.pathname.replace(/\/$/, '') || '/';
const isActive = (href: string) => pathname === href || pathname.startsWith(href + '/');
---

<header class="nav">
  <div class="wrap nav-inner">
    <a class="brand" href="/" aria-label="Northwest Onchain home">
      <Logo variant="dark" size={30} />
    </a>

    <nav class="nav-links" id="navlinks" aria-label="Primary">
      {
        NAV.map((item) => (
          <a href={item.href} aria-current={isActive(item.href) ? 'page' : undefined}>
            {item.label}
          </a>
        ))
      }
      <a class="nav-cta" href="/contact" aria-current={isActive('/contact') ? 'page' : undefined}>
        {BOOKING.ctaLabel}
      </a>
    </nav>

    <button
      class="nav-toggle"
      type="button"
      aria-label="Toggle menu"
      aria-expanded="false"
      aria-controls="navlinks"
    >
      <svg
        width="24"
        height="24"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        aria-hidden="true"
      >
        <path d="M3 6h18M3 12h18M3 18h18"></path>
      </svg>
    </button>
  </div>
</header>

<!-- External (not bundled-inline) so the CSP stays script-src 'self'. -->
<script src="/nav.js" defer is:inline></script>
