---
/**
 * Prose — typographic wrapper for rendered MDX. Brand-styled dark reading
 * surface (see .prose in global.css). Tables and definitions are styled with
 * care because answer engines disproportionately cite them.
 */
---

<div class="prose">
  <slot />
</div>
